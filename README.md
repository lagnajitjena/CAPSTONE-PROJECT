Network File Sharing Server & Client

This project uses Socket Programming using C++ to share files between Server and Client

Build and Run
1. Compile:
   
   g++ server.cpp -o server
   
   g++ client.cpp -o client

2. Run the server:
   ./server

3. Run the client (in another terminal):
   ./client 127.0.0.1 9000

4. Login credentials:

   Username: test
   
   Password: password123

